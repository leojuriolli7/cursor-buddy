{
  "name": "cursor-buddy-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter cursor-buddy build",
    "dev": "pnpm --filter cursor-buddy dev",
    "dev:demo": "pnpm --filter nextjs-demo dev",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
